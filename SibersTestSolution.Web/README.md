# SibersTestSolution.Web

React frontend for the Sibers test solution.

## Run

```powershell
npm.cmd install
npm.cmd run dev
```

By default Vite proxies `/api` to `http://localhost:5000`.
Use `VITE_API_PROXY_TARGET` if the API runs on another address.
